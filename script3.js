/* Snack3 
Crea un array di numeri interi e fai la somma di tutti gli elementi  
che sono in posizione dispari.


1. Crea un array di numeri interi 
2. prendere tutti i numeri dispari e metterli dentro ad un array 
3. sommare un array
*/

const numeri = [10, 5, 50, 2, 30, 24, 15, 75];
let sommaDispari = []

for (let i = 1; i < 10; i++){
   
    if (i % 3 == 0) {
      dispari = i;
    }
  }
    numeri=[];  
    
    for(let i = 1; i<numeri.length; i++){
        numeri += numeri[i];
        i++;
    }
    let text = numeri.toString();
    
    numeridispari.innerHTML = 'La somme degli indici dispari è: ' + i;
    
    console.log(sommaDispari);

 /* DA FARE IL 3 XK NON VIENE */ 
